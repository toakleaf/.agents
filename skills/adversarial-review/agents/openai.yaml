interface:
  display_name: "Adversarial Review"
  short_description: "Run independent adversarial review rounds"
  default_prompt: "Use $adversarial-review to run independent adversarial reviewers on the requested target."
policy:
  allow_implicit_invocation: false
